using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class CS_SeekingAttention : BrainStateBase
{
   /*

    public float onHoverWaitDur = 0.25f;

    protected override void Cb_TouchStateChangeEvent(CustomerBase.CustomerTouchState obj)
    {
        if (!active) return;
        if (obj == CustomerBase.CustomerTouchState.TopHover)
            StartCoroutine(AboutToSpeak());
    }

    IEnumerator AboutToSpeak()
    {
        for (float i = 0; i < onHoverWaitDur; i += Time.deltaTime)
        {
            if (baseModule.customerTouchState == CustomerBase.CustomerTouchState.NoHover)
            {
                yield break;
            }
            yield return null;
        }
        Callout();
        yield return null;
    }

    void Callout()
    {
        speech.SetSpeech(brain.data.Callouts.GetRandom());
    }
   */


}
